version: "3.1"

# Минимальная сторя для валидатора (intent inform используется внутри формы).
# Диалоговая политика — только RulePolicy без TED и без MemoizationPolicy (см. config.yml).

stories:

  - story: Форма принимает inform
    steps:
      - intent: start_interview
      - action: action_mark_screening_started
      - action: interview_form
      - active_loop: interview_form
      - intent: inform
      - action: interview_form
